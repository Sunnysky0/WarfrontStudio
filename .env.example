# Copy to .env.local. Start the DB with `npm run db:up`, then `npm run db:push`.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
# Keep loopback off the system VPN proxy (127.0.0.1:18081) so local Postgres works.
NO_PROXY=localhost,127.0.0.1
no_proxy=localhost,127.0.0.1
